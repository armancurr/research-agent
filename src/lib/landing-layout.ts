/** Full width of the hero background frame (bordered area with `background.jpg`). */
export const LANDING_SECTION_MAX_WIDTH = "max-w-[1450px]";

/** Inner max width for the chat preview inside the hero frame. */
export const LANDING_CONTENT_MAX_WIDTH = "max-w-[min(100%,1320px)]";
